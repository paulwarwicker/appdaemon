automationlib:
  module: automationlib
  global: true

# test:
#   module: test
#   class: Test
#   dependencies:
#     - automationlib

announcer:
  module: announcer
  class: Announcer
  dependencies:
    - automationlib

automation:
  module: automation
  class: Automation
  dependencies:
    - automationlib
    - announcer
    - timestamp

timestamp:
  module: timestamp
  class: Timestamp
  dependencies:
    - automationlib
    - announcer

garage:
  module: garage
  class: Garage
  dependencies:
    - automationlib
    - announcer

location:
  module: location
  class: Location
  dependencies:
    - automationlib
    - announcer
    - timestamp

alarms:
  module: alarms
  class: Alarms
  log_level: DEBUG
  dependencies:
    - automationlib
    - announcer

tap:
  module: tap
  class: Tap
  # log_level: DEBUG
  dependencies:
    - automationlib
    - announcer

sonos:
  module: sonos
  class: Sonos
  dependencies:
    - automationlib
    - announcer

weather:
  module: weather
  class: Weather
  log_level: DEBUG
  # location: 78704 US
  dependencies:
    # - automationlib
    - announcer

starling:
  module: starling
  class: Starling
  dependencies:
    - automationlib
    - announcer

timers:
  module: timers
  class: Timers
  dependencies:
    - automationlib
    - announcer

car:
  module: car
  class: Car
  dependencies:
    - automationlib
    - announcer

# motion:
#   module: motion
#   class: Motion
#   dependencies:
#     - automationlib
#     - announcer

